# ============================================================================
# 核心 Secret - LLM API 配置
# ============================================================================
# 这是必须的配置，包含 LLM API Key
#
# 使用方式:
#   1. 复制此文件或直接编辑
#   2. 填入真实的 API Key
#   3. kubectl apply -f secrets/core.yaml
# ============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: aiops-secret
  namespace: aiops
type: Opaque
stringData:
  # ==================== LLM 配置 ====================
  # DeepSeek API (推荐)
  DEEPSEEK_API_KEY: "sk-aa78473c067e4d80adcff95478d7bde3"
  
  # 或者使用 OpenAI
  # OPENAI_API_KEY: "sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
  
  # 或者使用 Azure OpenAI
  # AZURE_API_KEY: "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
  # AZURE_API_BASE: "https://your-resource.openai.azure.com"
  # AZURE_API_VERSION: "2024-02-15-preview"

