# ============================================================================
# 可观测性 Secret - Prometheus/Grafana/ELK 配置
# ============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: aiops-observability-secrets
  namespace: aiops
type: Opaque
stringData:
  # ==================== Prometheus ====================
  PROMETHEUS_URL: "http://prometheus:9090"
  # PROMETHEUS_USERNAME: ""
  # PROMETHEUS_PASSWORD: ""
  
  # ==================== Grafana ====================
  GRAFANA_URL: "http://grafana:3000"
  GRAFANA_API_KEY: ""
  
  # ==================== Elasticsearch ====================
  # ELASTICSEARCH_URL: "https://elasticsearch:9200"
  # ELASTICSEARCH_USERNAME: "elastic"
  # ELASTICSEARCH_PASSWORD: ""

